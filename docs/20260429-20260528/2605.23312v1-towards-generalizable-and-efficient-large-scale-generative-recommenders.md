---
title: Towards Generalizable and Efficient Large-Scale Generative Recommenders
title_zh: 迈向可泛化且高效的大规模生成式推荐系统
authors: "Qiuling Xu, Ko-Jen Hsiao, Moumita Bhattacharya"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23312v1"
tags: ["query:gen-rec"]
score: 10.0
evidence: 将生成式推荐模型从2M扩展到1B参数的生产实践
tldr: 在大规模生成式推荐中，预训练增益难以直接转化为生产环境下的下游任务改进，受任务天花板、重复训练成本、服务延迟及物品冷启动等制约。本文提出多标记预测、采样softmax与投影解码头以提升训练和推理效率，并通过语义物品塔与协作嵌入掩码解决冷启动问题。在涵盖百万用户的为期一周的生产影子评估中，1B参数模型在所有任务上均获得优于2M基线的MRR。结果表明，模型规模应作为生产迁移的一个组件，与任务天花板、解码成本、延迟对齐和物品泛化等因素综合考虑。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23312v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1604, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1197, \"height\": 862, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1026, \"height\": 390, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1211, \"height\": 647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1314, \"height\": 704, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 915, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23312v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1119, \"height\": 731, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.23312v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1268, \"height\": 259, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23312v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 305, \"label\": \"Table\"}]"
motivation: 预训练生成推荐模型在生产中面临任务天花板、重复训练成本高、解码延迟及物品冷启动等迁移瓶颈。
method: 通过多标记预测、采样softmax与投影解码头实现高效训练和推理对齐，采用语义物品塔与协作嵌入掩码处理物品冷启动。
result: 在百万用户的生产影子评估中，1B参数模型在所有报告任务上取得比2M基线更高的MRR。
conclusion: 模型规模应作为生产迁移问题的一个维度，与任务天花板、解码成本、延迟对齐和物品泛化共同优化。
---

## 摘要
生成式推荐模型能够将用户行为建模为事件序列，并为多个推荐任务提供共享的骨干网络。然而在生产环境中，预训练的收益并不会自动转化为下游应用的提升：任务提升空间、重复训练成本、服务延迟以及物品新鲜度都会影响迁移效果。我们描述了在生产级标题推荐场景中，将生成式推荐模型的骨干参数（不包括嵌入层和解码层）从200万扩展至10亿的经验。在多个下游任务中，我们观察到任务依赖的扩展行为：部分任务在观测的规模范围内接近经验上限，而其他任务则持续受益于额外的容量。这促使我们将偏移的缩放定律拟合作为诊断工具，以判断额外模型规模在何处可能更有用或更无用。我们随后研究了将模型应用于实践时产生的生产约束。对数万亿行为令牌的频繁重训练使得训练和解码效率至关重要；缓存服务可能使直接的下一个令牌目标变得陈旧；新上线标题在协同ID嵌入尚不可靠之前，可能需要通过语义元数据进行评分。我们通过多项令牌预测实现服务延迟对齐，采用采样softmax和投影解码头实现高效重复训练，以及使用带有协同嵌入掩码的语义物品塔实现冷启动适配，来解决这些问题。在超过100万用户、为期一周的生产镜像评估中，10亿骨干模型在所有报告任务上均取得了比200万骨干基线更高的平均倒数排名（MRR）。总体而言，结果支持将模型规模视为生产迁移问题的一个组成部分，与任务提升空间、解码成本、服务延迟对齐以及物品泛化并重。

## Abstract
Generative recommendation models can model user behavior as sequences of events and provide a shared backbone for multiple recommendation tasks. In production, however, pre-training gains do not automatically translate into downstream application improvements: task headroom, repeated-training cost, serving latency, and item freshness all affect transfer. We describe our experience scaling a generative recommender from 2M to 1B backbone parameters, excluding embedding and decoding layers, in a production-scale title recommendation setting. Across multiple downstream tasks, we observe task-dependent scaling behavior: some tasks approach an empirical ceiling within the observed scale range, while others continue to benefit from additional capacity. This motivates using offset scaling-law fits as a diagnostic for where additional model scale may be more or less useful.   We then study production constraints that arise when applying the model in practice. Frequent retraining over trillions of behavior tokens makes training and decoding efficiency important; cached serving can make the immediate next-token target stale; and newly launched titles may need to be scored from semantic metadata before collaborative ID embeddings are reliable. We address these issues with multi-token prediction for serving-latency alignment, sampled softmax and a projected decoding head for efficient repeated training, and semantic item towers with collaborative-embedding masking for cold-start adaptation. In a one-week production-shadow evaluation over 1M users, the 1B-backbone model achieves higher MRR than the 2M-backbone baseline across all reported tasks. Overall, the results support treating model scale as one component of a production transfer problem, alongside task headroom, decoding cost, serving-latency alignment, and item generalization.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将以中文、使用 Markdown 格式，对所提供的论文《迈向可泛化且高效的大规模生成式推荐系统》进行结构化、深入且客观的总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：论文探讨了在工业级生产环境中，将大规模生成式推荐模型从研究原型成功部署到应用时所面临的“迁移鸿沟”。
    *   **研究动机**：简单地增大生成式推荐模型的规模（例如，将参数从百万级提升到十亿级）并不能保证在真实生产环境中的下游任务质量得到相应提升。模型增益的转化受到多种现实因素的制约。
*   **关键挑战**：
    *   **任务天花板**：不同推荐任务的难度和可预测性不同，部分任务可能很快触及性能上限，单纯的模型扩容不再有效。
    *   **训练与推理效率**：模型需要基于数万亿行为令牌频繁重复训练以适应数据分布变化，导致高昂的计算成本，尤其是解码层。
    *   **服务延迟不匹配**：生产系统常使用缓存结果来降低延迟，导致模型训练的即时“下一令牌”目标与用户实际看到推荐时的状态存在时间差，目标变得“陈旧”。
    *   **物品冷启动**：新上线的标题缺乏足够的用户交互历史，其协同ID嵌入不可靠，需要依赖内容或元数据进行有效评分。
*   **整体含义**：论文主张不应孤立地看待模型规模，而应将其视为一个“生产迁移问题”的组成部分。成功的工业级生成式推荐系统，需要将模型容量与任务天花板、解码效率、服务延迟对齐和物品泛化能力等现实约束进行系统性的协同优化。

### 2. 论文提出的方法论

论文提出了一系列应对生产环境挑战的技术方法，其核心思想在于保证模型核心能力的同时，优化其效率、对齐服务目标并增强泛化性。

*   **2.1 任务依赖的缩放定律分析**
    *   **核心思想**：使用带偏移量的幂律函数来拟合模型规模与下游任务性能的关系，识别不同任务的经验性能天花板。
    *   **公式**：`P(N) = P0 - (N/N0)^(-a)`
        *   `P(N)` 是参数规模为 `N` 时的验证性能（MRR）。
        *   `P0` 是拟合出的饱和性能水平，代表在当前数据和目标下的经验天花板。
        *   该方法比对数线性模型更能准确地对有界指标和不同任务天花板进行建模，可用于诊断哪些任务仍能从扩容中受益。

*   **2.2 高效解码策略**
    *   **核心思想**：在不影响骨干网络容量的前提下，大幅降低输出评分的计算成本，使频繁重训练在操作上可行。
    *   **方法**：
        1.  **采样 Softmax**：训练时仅在正样本和随机均匀采样的 1% 负样本上计算 logits，而非全量候选集。
        2.  **共享投影解码头**：在logit计算前，通过一个共享的线性层将骨干网络的隐藏状态维度从 `d` 投影到 `d/8`，直接减少了矩阵乘法的规模。

*   **2.3 多令牌预测 (MTP)**
    *   **核心思想**：解决训练目标（即时下一令牌）与服务目标（缓存延迟后的相关目标）之间的不匹配问题，并对推荐的“集合性”（未来多个物品都可能有效）进行建模。
    *   **技术细节**：
        *   构建带权重的未来高价值目标标签集 `Y`。
        *   对标签集应用指数时间衰减，半衰期为一小时，使模型更关注临近服务状态的未来行为。
        *   优化一个加权多标签目标损失函数：`L = - Σ yi∈Y wi log pθ(yi | x)`
            *   权重 `wi` 由奖励信号 `ri`（如观看时长）和时间衰减因子共同决定。
        *   **关键优势**：在服务时，模型仍只需一次前向传播计算候选集分数，无需迭代生成。

*   **2.4 冷启动标题处理**
    *   **核心思想**：在保持成熟标题协同ID嵌入的同时，引入多模态语义物品塔，使解码器能在ID证据不可靠时，转而依赖物品的内容和元数据进行评分。
    *   **架构**：
        *   标题的**语义元数据**（如知识图谱特征、LLM2Vec语言嵌入、人工标注特征）在经过函数 `ϕsem` 映射后，得到一个语义表示 `zi`。
        *   **候选标题向量** `vi` 由协同ID嵌入 `eIDi` 和语义表示 `zi` 共同构成。对于冷启动标题，其ID嵌入被替换为一个可学习的**词汇外 (OOV) 嵌入**。
        *   **训练掩码机制**：在预训练期间，随机将输入侧或输出侧的协同ID嵌入替换为OOV嵌入，强制解码器学习融合语义证据与ID证据，为处理未见过的标题做好准备。

### 3. 实验设计

*   **评估场景**：生产级标题推荐（如 Netflix）。
*   **任务定义**：将推荐任务分为三类匿名类别，以观察不同可预测性下的行为差异。
    *   **任务 A**：低可预测性、长周期的品味任务。
    *   **任务 B**：中等可预测性、短周期的参与度任务。
    *   **任务 C**：高可预测性、由时间或可用性驱动的任务。
    *   **冷启动**：针对不在模型词汇表中的全新标题的评分能力。
*   **基准对比**：所有实验均与一个骨干参数为 200 万的基线模型进行比较。
*   **评估方法**：
    *   **缩放定律研究**：在受控时间划分的评估集上，训练并评估不同规模的模型（100K、1M、10M、100M、1B 参数）。
    *   **组件评估**：
        *   **MTP有效性**：通过回放实验，模拟在线服务（<1秒延迟）和48小时缓存服务的场景，对比NTP与MTP的相对MRR变化。
        *   **效率分析**：通过解析方法估算不同解码策略（全量softmax、采样softmax、投影响头）的训练 FLOPs。
    *   **最终系统评估**：在一个为期一周的**生产镜像评估**中，对超过100万用户进行流量镜像。模型输出被用于所有下游集成路径，但不影响用户体验，最终基于收集的下游标签进行评分。

### 4. 资源与算力

*   论文**未明确提及**所用的具体 GPU 型号、数量或单次训练的具体时长。
*   文中提到**预训练数据集包含 2 万亿行为令牌**，并强调了这是一个需要频繁刷新的训练任务。
*   通过解析估算，描述了**输出层优化的巨大算力节省**：当输出大小为 `10^6` 时，采样Softmax加投影头可将单令牌训练FLOPs降低约 **35.5倍**；在 `10^7` 量级时，降低约 **249倍**。这种规模的节省是使得数十亿参数模型的“反复重训在操作上可行”的关键。
*   论文提到了采用“从小到大的实验漏斗”策略，即先在较小模型上验证，再扩展到昂贵的十亿参数级训练，以节约开发成本。

### 5. 实验数量与充分性

*   **实验组数**：
    *   **缩放定律拟合**：在三种任务类别上，对五种模型规模进行了拟合，并与对数线性模型进行了对比。
    *   **解码效率分析**：对不同词汇表大小下的多种解码策略进行了估算对比。
    *   **服务延迟回放**：模拟了五种服务延迟（在线，1/4/24/48小时）下的性能衰减。
    *   **MTP消融**：对比了NTP与四种未来窗口大小（2-5个令牌）在两种服务场景下的效果。
    *   **最终生产评估**：对最终集成的1B模型与2M基线进行了大规模影子测试。
*   **充分性与公平性**：实验设计是充分的且公平的。
    *   **控制变量严格**：缩放定律研究中的训练数据、评估样本、词表等均保持不变，仅变更骨干网络大小。
    *   **评估指标客观**：主要使用连续且有界的 MRR，避免了阈值指标可能造成的“性能跃进”假象。
    *   **消融清晰**：对MTP的评估不仅验证其本身，还关联了服务延迟场景，体现了对生产约束的深刻理解。
    *   **最终验证权威**：生产镜像评估是工业研究的黄金标准，其在真实、大规模的复杂环境中验证了所有设计选择的综合有效性。

### 6. 论文的主要结论与发现

*   **扩展增益是非均匀的**：不同推荐任务遵循不同的缩放轨迹并具有不同的经验天花板。缩放定律可作为诊断工具，识别出哪里需要更多容量，哪里可能需要更优目标或更好的表征。
*   **效率优化使大规模扩展成为可能**：通过简单的采样softmax和投影解码头，可以极大地降低训练算力成本，使得在万亿级令牌上频繁重训练大型生成推荐模型变得可行。
*   **MTP对齐了训练与服务**：多令牌预测显著提升了模型在缓存服务场景下的表现，尤其对短周期任务和延迟容忍场景有效，但在在线、顺序敏感的任务中可能适得其反。
*   **语义塔解决了冷启动**：结合语义物品塔和协同嵌入掩码是处理新标题的有效方法，1B模型在冷启动标题上取得了相对于2M基线 28.1% 的最大提升。
*   **整体系统取得一致收益**：在百万用户级的生产镜像评估中，集成了所有方法的1B骨干模型在所有报告任务上均超越了2M基线，验证了将规模、效率、延迟和泛化进行协同设计的有效性。

### 7. 优点

*   **以生产为中心的问题定义**：论文的出发点不是纯粹的学术性能提升，而是解决工业界真实存在的“最后一公里”部署问题，具有很高的实用价值。
*   **任务天花板诊断**：引入的偏移缩放定律分析，提供了一个比简单报告“扩展成功/失败”更细致的视角，能有效指导资源投入方向。
*   **务实的效率设计**：提出的解码优化方案（采样Softmax + 投影头）简单而有效，易于实现，同时为计算资源节省给出了清晰的量化依据。
*   **服务感知的目标设计**：MTP是一个精巧的创新，它直接解决了许多工业系统中固有的、但鲜有模型设计深入考虑的“缓存陈旧性”问题。
*   **全面的系统协同设计**：论文并非孤立地提出技术点，而是呈现了一个将模型规模、训练效率、服务目标和对和物品泛化融为一体的完整解决方案。

### 8. 不足与局限

*   **效率指标的局限性**：训练效率的提升主要通过解析方法估算的“每令牌FLOPs”，而非报告端到端的实际训练吞吐量或墙钟时间，可能无法完全反映分布式训练中的通信等开销。
*   **缺乏与其他生成式范式的直接对比**：论文提到其设计与编码器-解码器架构、语义ID等方法互补，但未在相同规模下与这些方案进行直接的性能或效率比较。
*   **MTP的适应性**：MTP对在线、顺序敏感的任务造成了性能下降，表明它并非一个通用的替代方案，其应用需要根据任务和延迟特性进行取舍，文章对此的深入分析相对有限。
*   **冷启动的离线评估局限**：冷启动标题的评估仅限于影子测试中的离线指标，缺乏在线A/B测试中的用户反馈（如曝光率、转化率）来验证其在实际探索-利用权衡中的价值。
*   **细节披露有限**：作为工业论文，某些实现细节（如具体的元数据源、Tranformer架构变体）可能由于商业机密而未被充分披露，降低了直接复现的可能性。

（完）
