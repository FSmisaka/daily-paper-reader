---
title: "DeGRe: Dense-supervised Generative Reranking for Recommendation"
title_zh: DeGRe：面向推荐的稠密监督生成式重排序
authors: "Chaotian Song, Jingyao Zhang, Chenghao Chen, Zisen Sang, Dehai Zhao, Guodong Cao, Boxi Wu, Deng Cai, Jia Jia"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25749v1"
tags: ["query:gen-rec"]
score: 9.0
evidence: 密集监督的生成式重排用于推荐
tldr: 推荐重排需探索巨大排列空间，现有生成式方法面临启发式标签偏差与稀疏奖励问题。DeGRe通过离线-在线解耦设计，利用Lookahead Evaluator产生稠密监督信号蒸馏至在线生成器，使贪心解码逼近全局最优。在公开和工业数据集上超越基线，并已在淘宝闪购成功部署，提升在线推荐效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25749v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25749v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1498, \"height\": 764, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25749v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1450, \"height\": 779, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25749v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 884, \"height\": 204, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25749v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 877, \"height\": 367, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25749v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 900, \"height\": 218, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25749v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1731, \"height\": 393, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25749v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 835, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25749v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 641, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25749v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 754, \"height\": 219, \"label\": \"Table\"}]"
motivation: 解决生成式重排中启发式标签偏差和列表级稀疏奖励导致的信用分配困难。
method: 设计离线-在线解耦框架，离线用累积回归Lookahead Evaluator主动挖掘高价值序列，转化为步骤级稠密监督，蒸馏给轻量在线生成器。
result: 在公开基准和工业数据集上均优于基线模型，并于淘宝闪购上线取得显著收益。
conclusion: DeGRe通过稠密监督蒸馏使在线生成器具备前瞻规划能力，以高效贪心解码实现近似全局最优，兼具效果与效率。
---

## 摘要
在多阶段推荐系统中，重排序通过捕获列表内上下文依赖关系来优化整体效用，但其核心挑战在于在指数级排列空间中探索最优序列。近期研究转向端到端的生成式框架，通常利用列表级奖励或偏好对齐来指导生成器训练。然而，这些方法仍然面临两个关键问题。第一是启发式标签偏差。现有方法通常基于简单规则构建训练目标，例如将点击项提升至顶部，而忽略列表上下文中的因果依赖关系。第二是信用分配问题。稀疏的列表级后验奖励无法直接指导序列生成中的中间步骤，导致优化方向模糊。为解决这些问题，我们提出DeGRe（稠密监督生成式重排序），一个通过稠密监督弥合离线探索与在线效率之间差距的生成式重排序框架。DeGRe的核心在于其在线-离线解耦设计。在离线阶段，我们引入基于累积回归的前瞻评估器，利用束搜索主动挖掘未曝光空间中的高价值前瞻序列。在训练过程中，我们将评估器的逐步价值估计转化为稠密监督信号，并蒸馏到轻量级的在线生成器中。该机制使生成器能够内化前瞻规划能力，在线推理时只需一次高效的贪心解码即可逼近全局最优。实验表明，DeGRe在公开基准和工业数据集上均优于基线模型。我们已将DeGRe成功部署于淘宝闪购，显著提升了在线推荐效果。

## Abstract
In multi-stage recommender systems, reranking optimizes overall utility by capturing intra-list contextual dependencies, yet its central challenge lies in exploring optimal sequences within an exponentially large permutation space. Recent studies have shifted towards end-to-end generative frameworks, which typically leverage list-wise rewards or preference alignment to guide generator training. However, these methods still face two critical issues. First is the heuristic label bias. Existing methods often construct training targets based on simple rules, such as promoting clicked items to the top, while ignoring causal dependencies within the list context. Second is the credit assignment problem. Sparse list-level posterior rewards fail to directly guide intermediate steps in sequence generation, leading to ambiguous optimization directions.   To address these issues, we propose DeGRe (Dense-supervised Generative Reranking), a generative reranking framework that bridges the gap between offline exploration and online efficiency through dense supervision. The core of DeGRe lies in its offline-online decoupled design. During the offline phase, we introduce a Lookahead Evaluator based on cumulative regression, which leverages beam search to actively mine high-value lookahead sequences in the unexposed space. During training, we transform the step-wise value estimations from the evaluator into dense supervision signals and distill them into a lightweight Online Generator. This mechanism enables the generator to internalize lookahead planning capabilities, requiring only a single efficient greedy decoding pass during online inference to approximate the global optimum. Experiments demonstrate that DeGRe outperforms baseline models on public benchmarks and industrial datasets. We have successfully deployed DeGRe on Taobao Flash Shopping, significantly improving online recommendations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **研究背景与动机**:
    *   在工业级推荐系统（如淘宝）的多阶段架构中，重排序（Reranking）位于最后环节，旨在通过建模列表中物品间的上下文依赖（如互斥、互补），从排序阶段给出的候选集中生成一个最优的最终展示列表。
    *   **核心挑战**: 需要在指数级大小的排列空间（如12选6）中，找到能最大化整体效用（如总点击、总交易额）的最优序列。
    *   **现有方法的局限性**:
        *   **单阶段评估-后重排**：容易收敛到次优解。
        *   **两阶段生成器-评估器范式**：存在**目标不一致**、系统复杂度高、在线延迟大等问题。
        *   **现有端到端生成式重排方法（如GReF, NLGR, GoalRank）面临两个新瓶颈**:
            1.  **启发式标签偏差**: 训练标签基于简单规则（如将被点击物品置顶）构建，忽略了列表内的因果依赖，使模型学习有偏分布，无法探索未曝光空间的高价值序列。
            2.  **信用分配问题**: 仅依赖列表级的总奖励（如CTR/GMV）作为监督信号过于稀疏，无法为序列生成的每一步局部决策提供精确指导，导致优化方向模糊。

*   **整体含义与解决方案**:
    *   本文提出**DeGRe（稠密监督的生成式重排序）** 框架，旨在解决上述偏差与信用分配问题。
    *   其核心思想是“**离线-在线解耦**”：将计算密集的排列空间探索任务放到离线阶段，为在线阶段生成**稠密的、步骤级**的监督信号，从而让一个轻量级的在线生成器能以内化的“前瞻规划”能力高效推理，用一次贪心解码就逼近全局最优。

### 2. 论文提出的方法论

DeGRe由两个核心组件构成，并在三个不同阶段协同工作：**Lookahead Evaluator (DeGRe-E)** 和 **Online Generator (DeGRe-G)**。

*   **核心思想：离线-在线解耦与稠密监督**
    *   **离线阶段**：利用丰富的计算资源，通过评估器主动挖掘未曝光空间中的高价值序列，并构建步骤级的稠密监督信号。
    *   **训练阶段**：通过混合蒸馏，将这些稠密信号“注入”到轻量级的在线生成器中，使其内化前瞻规划能力。
    *   **在线阶段**：仅部署生成器，通过一次高效的贪心解码生成推荐列表，满足低延迟要求。

*   **关键技术细节**:

    *   **1. Lookahead Evaluator (DeGRe-E)**:
        *   **架构**: 基于因果Transformer的**累积回归模型**。
        *   **功能**: 接收任意长度的子序列，输出其**累积价值分布**的估计。具体来说，它不是直接回归标量价值，而是预测当前累积价值（如点击数）达到阈值\( K \)的概率 \( P(V \ge K | l_{1:t}) \)。
        *   **训练目标**: 使用有序二元交叉熵（BCE）损失，将累积价值回归分解为一系列有序的二分类问题。

    *   **2. 稠密监督构建 (离线阶段核心)**:
        *   **前瞻序列挖掘**: 使用训练好的评估器 \( E_{\phi} \) 执行**束搜索**，在候选集的排列空间中进行探索，生成一系列高价值的前瞻序列集合 \( T_{syn} \)。
        *   **稠密标签构建**: 对于每条前瞻序列，在生成步骤 \( t \)：
            *   **硬标签**: 评估器在该步实际选择的物品 \( v_{i_t} \)，作为生成器需要模仿的确定性决策。
            *   **软标签**: 基于评估器对所有剩余候选物品的价值估计，通过Softmax构建一个概率分布 \( q_t \)。该分布保留了细粒度的排序信息，作为局部正则化项。
        *   **序列加权**: 对不同前瞻序列，根据其预估的最终总价值分配不同权重 \( w_l \)，使得训练重点偏向高价值序列。

    *   **3. 高效在线生成器 (DeGRe-G)**:
        *   **架构**: 轻量级的编码器-解码器结构。
            *   **双向候选编码器**: 并行编码整个候选集，显式捕获物品间的竞争与互补关系。
            *   **用户引导的因果解码器**: 以用户表征而非静态起始符作为解码起点，实现个性化启动。解码过程使用因果自回归。
            *   **候选约束解码**: 类似指针网络，每一步的解码概率严格限制在\( V_u / l_{<t} \)中，保证不重复且始终从候选集中选择。

    *   **4. 训练与推理**:
        *   **混合蒸馏目标**: 生成器 \( G_{\theta} \)的训练损失是两项之和，并由序列权重\( w_l \)加权：
            *   \( L_{CE} \) (前瞻模仿): 拟合硬标签。
            *   \( \alpha L_{KL} \) (价值对齐): 拟合软标签分布 \( q_t \)。
        *   **推理**: 在线仅需部署 \( G_{\theta} \)，进行一次贪心解码。

### 3. 实验设计

*   **数据集**:
    *   **ML-1M**: 公开电影推荐数据集。
    *   **Taobao Ad**: 公开淘宝展示广告数据集。
    *   **Taobao Flash Shopping**: 淘宝闪购工业级数据集，包含数亿条用户交互记录，每天服务超1亿日活用户。

*   **对比方法 (Baselines)**:
    *   **生成器基线**:
        *   `NAR4Rec`: 非自回归生成模型。
        *   `GReF`: 统一生成式重排框架，集成RL对齐技术。
        *   `NLGR-G`: 使用反事实信号和采样的生成器。
        *   `GoalRank`: 基于“组相对优化”原则的生成式重排。
    *   **评估器基线**:
        *   `DeepFM`: 经典的点估计模型。
        *   `PIER`: 上下文感知的列表预测模型。
        *   `NLGR-E`: NLGR的评估器组件。

*   **评估指标**:
    *   **生成器**: `HR@K%`（命中率），使用蒙特卡洛采样评估生成列表的质量。
    *   **评估器**: `R-AUC`（回归AUC）、`PCOC`（校准度）、`RMSE`（均方根误差）。
    *   **在线A/B测试**: `CTR`（点击率）、`ORDER`（订单率）、`GMV`（成交总额）、推理延迟。

### 4. 资源与算力

*   **硬件**: 所有模型均使用NVIDIA A100 (80GB) GPU进行实现和训练。
*   **未明确说明的部分**: 文中没有提及所使用的精确GPU卡数、模型训练的迭代次数（Epochs）或总时长。

### 5. 实验数量与充分性

*   **实验数量与类型**:
    *   **主实验**:
        *   生成器性能对比：在3个数据集上进行了1组全面的对比实验（见表2）。
        *   评估器性能对比：在Taobao Flash Shopping数据集上进行了1组对比实验（见表3）。
    *   **消融实验**: 在`B=2`设置下，拆解了4个变体（无软标签、无序列加权、无硬标签、仅曝光数据），验证了各组件的有效性（见表4）。
    *   **超参数分析**: 分析了3个关键参数（束搜索宽度B、软标签权重α、序列加权温度τ）对性能的影响（见图4）。
    *   **在线A/B测试**: 在真实线上环境进行了为期8天的实验，并进行了不同用户组（新/老用户）和不同客户端场景（淘宝App/支付宝App）的鲁棒性分析。

*   **充分性、客观性与公平性**:
    *   **充分性**: 实验覆盖了公开与工业数据集、离线与在线评测、组件有效性与鲁棒性分析，设计较为全面。
    *   **公平性**: 生成器对比实验中，所有方法均使用独立的外部评估器在统一配置下评估，确保了对比的公平性。数据划分严格遵守时间因果关系，使用留一法分割。实验重复5次并取平均结果，增强了结果的可信度。

### 6. 论文的主要结论与发现

*   **性能领先**: DeGRe在公开和工业数据集上均显著超越所有现有SOTA基线模型。在束搜索增强下（B=8），其命中率（HR@1%）提升巨大，相比Baseline最高可提升53.19%。
*   **稠密监督有效性**: 前瞻序列挖掘和稠密监督（硬/软标签）是模型性能的基础。移除硬标签会导致性能崩溃，而软标签和序列加权也带来了正向但更细微的提升。
*   **在线业务价值**: 在淘宝闪购的线上A/B测试中，DeGRe相比Base策略，CTR提升+2.85%，ORDER提升+2.14%，GMV提升+3.75%。同时，相比经典的PRM模型也有显著优势。
*   **高效性与鲁棒性**: 得益于离线-在线解耦，在线推理仅增加14.8ms延迟，平衡了效果与效率。模型在新/老用户和不同客户端场景下均表现鲁棒。

### 7. 优点

*   **问题洞察深刻**: 精准指出了生成式重排中“启发式标签偏差”和“信用分配问题”两个核心痛点。
*   **方法论创新**: 提出的**离线-在线解耦**及**稠密监督蒸馏**范式，是解决探索与效率矛盾的优雅方案；利用**累积回归**进行步骤级价值估计，巧妙地解决了细粒度监督问题。
*   **实验扎实全面**: 包含多数据集、多基线离线对比、组件消融、超参数分析和完整的在线A/B测试及鲁棒性分析，论证链条完整，结论说服力强。
*   **工业实践价值高**: 方法设计考虑到工业界严格的延迟要求，并在顶级电商平台的亿级用户场景中成功部署，取得了显著的业务指标提升，证明了其巨大的应用潜力。

### 8. 不足与局限

*   **离线计算成本**: 离线阶段的束搜索和稠密标签构建需要大量计算，虽然论文提到可以通过Top-K截断和GPU并行来提升扩展性，但其对更大规模候选集（如\( N=100 \)）的离线探索成本未做评估。
*   **对评估器精度的依赖**: 整个系统的性能天花板由Lookahead Evaluator决定。如果评估器本身存在系统偏差（如对某些物品种类价值估计不准），该偏差会被蒸馏并放大到在线生成器中。
*   **超参数敏感性**: 论文指出性能对软标签权重α和序列加权温度\( \tau_w \)呈“倒U型”关系，意味着新场景部署时需要一定的调参成本。
*   **数据集多样性**: 虽然使用了不同数据集，但Taobao数据集均来自电商领域，在内容推荐（如新闻、短视频）等场景下的泛化性尚未充分验证。
*   **生成长度限制**: 在线生成器基于自回归，其推理时间与生成长度L成正比。虽然当前\( L<10 \)效率可接受，但应用于需要生成长序列（如榜单）的场景时，延迟问题可能会凸显。

（完）
